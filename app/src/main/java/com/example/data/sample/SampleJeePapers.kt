package com.example.data.sample

import com.example.data.model.QuestionItem

/**
 * Clean data holder. All sample/demo data has been removed.
 * Tests are created dynamically by the user uploading real JEE PDF papers and Answer Keys.
 */
object SampleJeePapers {
    fun getSamplePaper2025Jan(): List<QuestionItem> = emptyList()
    fun getSamplePaper2024Shift2(): List<QuestionItem> = emptyList()
    fun getQuizrrPartTest02(): List<QuestionItem> = emptyList()
}
