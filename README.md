# KARAM 2027

JEE CBT preparation app.

## Direct Gemini JSON import

Generate a complete JEE test JSON in Google AI Studio/Gemini, export the `.json` file, then import it directly inside KARAM. The app validates the JSON, stores the test locally, and makes it available in the Test Library without requiring a share URL or backend.
